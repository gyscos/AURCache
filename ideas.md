* When adding a git packages that replaces an AUR package, show at least a message that it can't be done. Better: ask if we should update the existing entry.
* Allow changing the package source in the settings of a package?
* Add a "dependencies failed" build status
* Show which dependencies we're waiting on
