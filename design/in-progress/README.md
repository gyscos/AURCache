# In-progress designs

Designs being implemented right now: partially built, under active revision.

Nothing lives here at the moment. Move a document here once work against it
starts, and on to `implemented/` once what it describes is built, noting any
part deliberately left out in its status line.
