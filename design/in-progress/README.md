# In-progress designs

Designs being implemented right now: partially built, under active revision.

- `worker-configuration.md` (with its two reviews) -- phase 1 steps 1-2 are
  built (workers declare and report their settings); setting them from the
  server (steps 3-6) is not.

Move a document to `implemented/` once what it describes is built, noting any
part deliberately left out in its status line.
